# Repository Guide

## 项目用途

这是一个开源项目源码阅读仓库，用于保存上游源码和中文学习笔记。主要工作是阅读、分析和总结 `code/` 中的项目，并将成果沉淀到 `notes/`。

## 目录结构

- `code/`：克隆的上游开源项目。除非用户明确要求修改，否则将其视为只读研究材料。
- `notes/`：按项目分类的源码分析和学习笔记。
- `projects.json`：正在研究的开源项目列表。
- `README.md`：仓库说明。

## 工作规则

- 默认使用中文编写说明和笔记；代码标识符、类名和 API 名称保留原文。
- 分析结论应以本地源码为依据，先定位定义、调用链、测试和示例，再形成结论。
- 笔记中的源码引用使用仓库相对路径，确保在仓库内可以跳转。
- 修改文件前，检查目标目录及其父目录是否存在更具体的 `AGENTS.md` 或 `AGENTS.override.md`，并同时遵守其规则。
- 不把推测写成确定事实；未完成、废弃、实验性或包含 `TODO`/`FIXME` 的实现需要明确标注。
- 除非用户明确要求，不修改 `code/` 下的上游代码，不执行提交、推送或其他外部发布操作。
- 新笔记放在 `notes/{project-name}/`；已有同主题笔记时优先补充，避免重复文件。
- 保留用户已有修改，不覆盖与当前任务无关的工作区内容。
- 搜索源码优先使用 `rg` 或 `rg --files`。
- 源码研究任务一般不需要构建整个上游项目；只有在结论需要运行验证或用户明确要求时才执行相应测试。

## 笔记格式

源码笔记应根据主题选取必要章节，通常包括：

1. 研究范围和源码版本。
2. 整体架构和核心抽象。
3. 关键类及职责。
4. 主要调用链、状态流转或执行流程。
5. 扩展机制和设计模式。
6. 源码中的限制、风险和注意事项。
7. 推荐阅读顺序或后续学习方向。

笔记以解释实现原理为主，避免简单堆砌类名或大段复制源码。复杂流程优先使用简洁的 ASCII 图、Mermaid 图或表格表达。

## 验证要求

- 确认新增或修改的 Markdown 链接指向存在的仓库文件。
- Markdown 文件使用 UTF-8 编码。
- 完成后检查 `git diff` 和 `git status`，确保没有混入无关修改。
- 如果运行了测试或构建，在交付说明中写明命令和结果；未运行时不要声称已经验证通过。

